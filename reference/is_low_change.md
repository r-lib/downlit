# Compare two recorded plots

Compare two recorded plots

## Usage

``` r
is_low_change(p1, p2)
```

## Arguments

- p1, p2:

  Plot results

## Value

Logical value indicating whether `p2` is a low-level update of `p1`.
